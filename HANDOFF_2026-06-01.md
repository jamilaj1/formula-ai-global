# 🤝 HANDOFF — Formula AI Global (2026-06-01)

> **For the next session / new chat.** Read this top-to-bottom first, then
> `PROJECT_CONTEXT_2026-06-01.md` (full master), `ROADMAP_TO_10.md` (the
> path-to-10 plan + progress log), and `BUILD_ROADMAP.md` (phase log).
> Repo: `H:\FormulaAI-Backup-2026-05-11\` · Remote:
> github.com/jamilaj1/formula-ai-global (main) · push = deploy.
> Live: https://jamilformula.com · Worker:
> formula-ai-brain.jamilaj1.workers.dev · Backend:
> formula-ai-chem.onrender.com · Supabase: ivabcssceeaqgqjzgmdx.

---

## 1. Working rules (re-read every session)
- **Verify before execute** — read live code/files before claiming state;
  cite file:line. (Old docs are stale; e.g. "docs.html is thin" was false,
  "7,000 chemicals" was false — chemicals_database is empty.)
- **Write complete code** — full files / proper Edit calls, never "replace
  X with Y" snippets.
- **Other-AI suggestions** — take the good, flag the wrong/stale, NEVER
  delete from the core project without explicit permission.
- **Commit + push every change** — push to `main` IS the deploy (CI builds
  Worker + frontend). Verify live with `curl -I`, not "CI green" alone.
- **After closing a phase, give a full report** (what shipped, what's
  unlocked, status, owner actions, next options).
- **GLOBAL brand, never regional.** Suppliers/buyers/users worldwide.
  Ghana = founder's plant + Paystack account (assets/facts), not a ceiling.
- **English-first deliverables**; translation on request. UI flips AR/EN
  via data-i18n-ar.
- **DosLunas = 50+ tons/DAY** (not month). The 2,000 t/mo figure (oversight)
  is separate + correct.
- **No fabricated claims** — owner catches them instantly (we hid the fake
  testimonials; built a real capture system instead).
- **SEO guardrail** — quality data-backed pages only, NEVER thin AI pages
  (Google penalty). Ship in batches.

## 2. Owner
Jamil Abduljalil (jamilaj1@gmail.com). 25-yr industrial chemist, not a
coder, writes Arabic (reply in Arabic + English mix). Oversees ~2,000
t/mo across countries + own DosLunas plant (50+ t/DAY) in Ghana. 60k+
Facebook followers = the launch audience. Goal: **$50K/month in 9-12mo**.

---

## 3. What we shipped this session (37 commits, last = 6215f76)

**All technical phases complete + the path-to-10 buildables:**

| Area | Shipped |
|---|---|
| **Phase 2 close** | Approve & deliver loop: markdown→PDF→Resend→delivered + resend |
| **Navbar redesign** | 33 pages unified: 6 primary + Tools dropdown (17), responsive; `scripts/sync_navbar.py` is the source of truth (edit PRIMARY/TOOLS, re-run) |
| **9.1 Vector RAG** | pgvector + 3,381 embedded (OpenAI) + match_formulas RPC; chat searches semantic-first |
| **9.2 Teams** | teams/members/invitations + RLS + invite email + team.html + accept-invite.html |
| **9.3 CSV/XLSX import** | FastAPI parser + workspace.html import modal |
| **9.4 Chat export** | /chat/export?format=md\|pdf + buttons in chat header |
| **9.5 Financials** | /be/admin/financials + admin tab (MRR/ARR/LTV/ARPU/margin) |
| **9.6 CSP** | removed 'unsafe-eval'; CI now ships .htaccess/robots/manifest |
| **9.7 Tests** | 218 vitest + ~60 pytest |
| **A1/A3/A4** | outcome-centric home hero + tool headers + enterprise.html |
| **B4** | share buttons (formula.html) + referral banner (dashboard) |
| **C4** | Open Graph/Twitter on consulting/enterprise/pricing/about/industries |
| **C1** | `scripts/build_chemicals.py` ready (GATED: chemicals_database empty) |
| **D2** | activation rate (first AI call in 24h) in Financials |
| **D3** | real testimonial capture: dashboard form → admin moderate → homepage |
| **E3** | sign-ups time-series chart in Financials |
| **E5** | client-side error capture → Sentry + Better Stack |
| **F2** | enterprise one-pager PDF (/be/enterprise/onepager) + download button |
| **Growth kit** | `marketing/VIDEO_SCRIPTS.md` (10), `marketing/FACEBOOK_POSTS.md` (12), `welcome.html` LP, `/stats/community` counter, UTM tracking |
| **Docs** | `PROJECT_CONTEXT_2026-06-01.md` (master + sitemap), `ROADMAP_TO_10.md` |

Tests: `npm test` (vitest) + `pytest backend/tests`. Build: `npm run build:worker`.

---

## 4. ⚠️ PENDING OWNER ACTIONS

### A. SQL migrations — ALL RUN ✅ (verified live 2026-06-01)
1. `2026-05-30_teams.sql` ✅
2. `2026-06-01_testimonials.sql` ✅ (table live, 0 rows — awaiting first real one)
3. `2026-06-01_signups_by_day.sql` ✅ (returns 31-day series)
4. `2026-06-01_activation_rate.sql` ✅ — **live result: 7 eligible, 3
   activated, 42.9% activation** (already above the 10/10 >40% target!)
**No SQL pending. Nothing technical is blocking.**

### B. Secrets set ✅: OPENAI_API_KEY (Worker), RESEND_API_KEY (Render).
Vector backfill done (3,381/3,381 embedded). Financials dashboard now
shows real MRR/ARR/LTV + Activation 42.9% + the sign-ups trend chart.

### C. Human-only growth work (no one else can do — THIS is the bottleneck):
- Record the 10 videos (face/voice in #1 & #9, screen recording for rest).
- Post the 12 FB posts (2-3/day) with welcome.html?utm_campaign=<tag> links.
- Collect REAL testimonials (the system is built).
- Provide a GLOBAL supplier roster + commission % (Phase 8).
- (Optional) populate chemicals_database (PubChem backfill) → run
  `python scripts/build_chemicals.py all` for per-chemical SEO pages.

### D. Routine: after any deploy, purge LSCache (hPanel → Cache Manager)
or wait 1h to see HTML changes live.

---

## 5. Status on the $50K roadmap
- ✅ Live: Phases 1, 2, 3, 4, 7, and ALL of 9 (9.1-9.7).
- ⏸️ Blocked on owner content: 5 (lead-gen), 6 (distribution), 8 (procurement).
- Path-to-10 scorecard (ROADMAP_TO_10): ~5 of 9 criteria met (was 2). The
  rest is earned by LAUNCHING, not building.

## 6. The honest bottom line
The product has **decisively crossed the launch threshold**. There is **no
remaining buildable, unblocked technical work** of real value. The bottleneck
is now 100% execution (run migrations, record videos, post, collect proof) —
which only Jamil can do. Next session: either (a) support the launch / tweak
content, (b) build per-chemical SEO once chemicals_database is populated, or
(c) start a blocked phase once Jamil supplies its content.

## 7. Key architecture quick-ref
- Worker routes: `/search /chat /chat/export /library/* /be/consulting/*
  /be/enterprise/* /be/team/* /be/admin/financials /be/testimonial(s)/*
  /be/client-error /stats/community` + proxies `/chem /agents /vision` → Render.
- Worker handlers live in `worker-src/handlers/`, registered in
  `worker-src/index.js`. Build bundles to `worker.js` (committed).
- Backend routers in `backend/app/api/v2/` (consulting, library_pdf,
  chat_export, library_import, enterprise_pdf, ...), wired in `backend/main.py`.
- Secrets: Worker (ANTHROPIC/OPENAI/SUPABASE/PAYSTACK/RESEND via Supabase
  pg_net/BACKEND_INTERNAL_SECRET/SENTRY/BETTER_STACK), Render (mirror +
  RESEND_API_KEY). `.env` at repo root (gitignored): SUPABASE + OPENAI.

_End of handoff. Everything is committed + pushed at 6215f76. Good luck._
