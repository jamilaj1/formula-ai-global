# Formula AI Global — Backend dependencies (Python 3.11+)

# ---------- API framework ----------
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.9.2

# ---------- Environment & config ----------
python-dotenv==1.0.1

# ---------- Data layer ----------
supabase==2.9.1
postgrest==0.17.2

# ---------- AI ----------
anthropic==0.39.0

# ---------- Payments ----------
stripe==11.1.1

# ---------- HTTP / scraping (knowledge collector) ----------
httpx==0.27.2
aiohttp==3.10.10
beautifulsoup4==4.12.3
lxml==5.3.0

# ---------- Document parsing (PDF / Excel / books) ----------
pypdf==5.0.1
openpyxl==3.1.5
python-docx==1.1.2

# ---------- Chemistry science layer (Phase 1 — RDKit-powered) ----------
# RDKit ships pre-compiled wheels for linux-x86_64 + macOS + Windows.
# These are REQUIRED for the /api/chem/* endpoints to work.
rdkit==2024.9.4
numpy==1.26.4
scipy==1.13.1
# Trained ML models (Phase 7 — Random Forests for logP / compatibility / stability)
scikit-learn==1.5.2
joblib==1.4.2
# DeepChem (heavyweight, optional — for ML-based property prediction in Phase 4)
# deepchem==2.8.0

# ---------- Caching ----------
redis==5.1.1

# ---------- Bots ----------
python-telegram-bot==21.6
twilio==9.3.5

# ---------- Background scheduling ----------
apscheduler==3.10.4

# ---------- Export (PDF / Excel reports) ----------
reportlab==4.2.5
xlsxwriter==3.2.0

# ---------- Logging / monitoring (optional) ----------
sentry-sdk==2.17.0

# ---------- Dev / testing ----------
pytest==8.3.3
pytest-asyncio==0.24.0
