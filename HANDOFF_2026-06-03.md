# 🤝 HANDOFF — Formula AI Global (2026-06-03)

> **For the next chat session.** Read this top-to-bottom FIRST, then
> `PROJECT_CONTEXT_2026-06-01.md` (the master state). This handoff captures
> the 2026-06-03 session deltas on top of that master.
> Repo: `H:\FormulaAI-Backup-2026-05-11\` · Remote: github.com/jamilaj1/formula-ai-global (main) · **push = deploy** (CI builds Worker + frontend via FTP).
> Live: https://jamilformula.com · Worker: formula-ai-brain.jamilaj1.workers.dev · Backend: formula-ai-chem.onrender.com · Supabase: ivabcssceeaqgqjzgmdx (eu-west-1, Pro).
> Owner: Jamil Abduljalil (jamilaj1@gmail.com), 25-yr industrial chemist, not a coder, writes Arabic (reply AR+EN). Goal: $50K/mo in 9–12mo.

---

## 1. Working rules (re-read every session)
- **ADVISOR MODE (owner directive, 2026-06-03):** Be his blunt strategic advisor, not a yes-man. Challenge weak ideas with evidence. On a plan, give the 3 biggest gaps BEFORE positives. Never say "great idea" without justification. Treat him as a founder betting his savings.
- **Verify before execute** — read live code/DB before claiming state; cite file:line. (Migrations exist in-repo that were NEVER applied — see §4.)
- **Write complete code** — full files / proper Edit calls, never "replace X with Y" snippets.
- **Commit + push every change** — push to `main` IS the deploy. Worker changes need `npm run build:worker` (esbuild → worker.js, committed) BEFORE push. Syntax-check inline HTML/JS with `node --check` before pushing (a broken app.js/formula.html breaks the whole site).
- **Migrations are MANUAL** — paste SQL into Supabase SQL Editor (Role: postgres); CI never applies DDL. After DDL, optionally `NOTIFY pgrst, 'reload schema';`.
- **No fabricated claims** — owner (and his 60k+ expert audience) catch them instantly.
- **GLOBAL brand, not regional.** Arabic-first for HIS audience, but worldwide.
- **LSCache (1h)** holds HTML. After a deploy, purge (hPanel → Cache Manager) OR append `&t=<n>` to the URL to bypass (great for testing). Assets use `?v=N` (auto-bumped by commit count) so they refresh on their own; the HTML referencing them is what gets cached.
- **Other-AI suggestions / never delete core without explicit permission.**

---

## 2. What this session shipped (all pushed to main; last commit `56d2145`)

**Detail page + safety + i18n (the big arc):**
| Area | What |
|---|---|
| Chat broken links | Chat formula chips/cards now link to `formula.html?id=<id>` from the worker's `formula_refs` (was building `?name=<description>` → 404). |
| Safety engine (`formula-detail-live.js` AND `formula.html`) | Fixed TEA false-positive (`/ethanol/i` matched "tri-ethanol-amine" → now `\bethanol\b`); added LABSA/sulfonic acid; **acids/hydroxides → DANGER by default, except a mild list** (citric/lactic/ascorbic/stearic/fatty/amino/hyaluronic/sorbic + Mg/Al hydroxide → caution); one hazard card per ingredient (highest severity). |
| **Detail-page consolidation** | TWO competing detail pages existed: `formula.html` (singular, where ALL links point — has share + batch scaler) vs `formulas.html` (plural → `formula-detail-live.js` — had the safety engine). **Consolidated onto `formula.html`** (ported the safety engine + AR function glossary into it). `formulas.html`/`formula-detail-live.js` is now an orphan (still works; could redirect later). |
| i18n (site-wide latent bug fixed) | `app.js` `faiApplyLang` was never exposed → `window.applyLang` and `window.applyI18n` were both undefined, so dynamic content never re-translated anywhere. **Fixed: `app.js` now exposes `window.applyLang`.** `formula.html` calls it after render. |
| **Full Arabic localization** | (a) Owner-validated **function glossary** (EN→AR) in the detail page; (b) **`/translate` worker endpoint** (EN→AR, Sonnet/Haiku + 30-day KV cache) translates free text (procedure, ingredient names, function long-tail) on the detail page when AR; (c) **site-wide auto-translate layer** in `app.js` (`autoTranslatePage()`): when AR, any untranslated leaf text/placeholder is batch-translated via `/translate` (guards skip numbers/CAS/codes/brand/already-Arabic; opt out with `data-no-translate`). |
| **Translation overrides (Step 3)** | Owner corrections beat AI. `/translate` reads `translation_overrides` first. Owner-only endpoints `/be/admin/translations` (GET), `/be/admin/translation` (POST upsert), `/be/admin/translation/delete`. New **"Translations" tab** in admin.html. |
| **Community submissions (contribute) fixed + moderated** | `contribute.html` was broken ("table user_submissions not found"). Created the table (migration was untracked in `backend/ingestion/schema_submissions.sql`). Built **moderation**: `/be/admin/submissions` (list pending), `/be/admin/submission/structure` (Sonnet extracts the formula, no save), `/be/admin/submission/publish` (insert OWNER-EDITED formula into `formulas` as `human_verified`, `source_type='community'`, trust 85), `/be/admin/submission/reject`. New **"Submissions" tab** in admin.html: review → editable clear formula (ingredient/%/function table) → publish or reject. **The credibility gate.** |
| Marketing | `marketing/HERO_VIDEO_AR.md`, `marketing/PAGE_VIDEOS_AR.md` (Arabic video scripts/storyboards). |
| Repo hygiene | Tracked `backend/ingestion/schema_submissions.sql` + `process_submissions.py` (were untracked). |

New worker handler files: `worker-src/handlers/translate.js`, `translations_admin.js`, `submissions_admin.js`. All routed in `worker-src/index.js`.

---

## 3. DB audit result (ran `select table_name from information_schema.tables`)
- ✅ All user-facing tables EXIST. No more "table not found" landmines for users.
- This session applied (owner ran in Supabase): `user_submissions` (+ queue view), `schema_v2` tables (`lab_simulations, safety_reports, learning_rules, knowledge_nodes, knowledge_edges, substitution_plans` — with RLS, service-role only).
- **Old/superseded `.sql` files exist in-repo (database/schema.sql, supabase_full_schema.sql, schema_extensions.sql) — IGNORE them** (legacy designs with old table names like `users`, `chat_history`).

---

## 4. ⚠️ PENDING OWNER ACTIONS
1. **Run `translation_overrides` table SQL** (if not done) — else the admin "Translations" tab can't save:
   ```sql
   create table if not exists translation_overrides (
     id uuid primary key default gen_random_uuid(),
     source_text text not null unique, ar_text text not null,
     updated_at timestamptz default now());
   alter table translation_overrides enable row level security;
   ```
   then `NOTIFY pgrst, 'reload schema';`
2. **Verify the Submissions moderation flow** end-to-end: admin.html (`&t=` bust) → Submissions → Review & edit → ingredients should populate (Sonnet extraction, just deployed `56d2145` — last thing tested; was returning empty components with Haiku, switched to Sonnet — CONFIRM it now extracts). Then Publish.
3. **Embedding backfill** for community-approved formulas so they show in SEMANTIC search (they're keyword-searchable immediately; vector needs an embedding — the existing backfill script/process). Low priority.
4. Routine: purge LSCache (or use `&t=`) after deploys.

---

## 5. STRATEGIC STATE (the real bottleneck)
- **Launched organically** (NOT paid): posted the launch to FB profile + 2 pages + the **51.2k private group "اسرار صناعة المنظفات"** that Jamil ADMINS (his crown asset — niche Arab/Egyptian SME detergent makers = his exact buyers).
- **~21+ qualified signups** in the first hours (factory-owners, R&D chemists, GMs, production managers — all `free` plan, 0 paid yet). Demand validated; payment NOT yet validated.
- **FIRST ACCEPTANCE** came from a lead **in Syria** → payment blocked (no Visa/Mastercard/Paystack in Syria). **Solutions:** (a) **recommended for this first one — do the consulting/help FREE in exchange for a testimonial + case study** (sidesteps payment, gets the first social proof); (b) if collecting: **USDT (TRC20)** is the working method for Syria, or a proxy payer abroad.
- **Competitive strategy (researched):** the real competitor is NOT enterprise R&D platforms (Albert Invent, Uncountable — wrong buyer) — it's **free ChatGPT/DeepSeek**. Win with: verified DB (not hallucinated) + Arabic + 25-yr-chemist trust + the 51k community. **Moat = trust + community + verified Arabic content (the contribute flywheel), NOT the tech features** (those are commoditizing). Positioning: "the trusted Arabic formulation authority + community," not "an AI platform." Strategy = **focus-differentiation** on Arabic SME detergent/home-care first; dominate the niche before expanding.
- **THE BOTTLENECK IS SELLING, NOT BUILDING.** The product is excellent + safe. Jamil's job: contact the 21 leads personally (factory-owners first — drafts were provided), close the first deal, collect the first testimonial, work the 51k group's comments (every engaged member = a warm consulting lead). Advise AGAINST more building unless it directly unblocks a sale.

---

## 6. Next-session options
- **(Recommended) Support the sale:** draft delivery for the first (Syrian) customer, refine outreach, help collect the testimonial → first case study.
- Verify/finish the Submissions Sonnet extraction; do the embedding backfill for community formulas.
- Bulk email later (Resend Broadcasts + Supabase emails + unsubscribe) — NOT for the 21 warm leads (personal converts better).
- Only build if it unblocks revenue. Default stance: **stop building, sell.**

---

## 7. Doc map
- **This file** = 2026-06-03 session entry point.
- `PROJECT_CONTEXT_2026-06-01.md` = master state (architecture, routes, tables, roadmap). Still accurate for the stable base; this handoff carries the 2026-06-03 deltas.
- `BUILD_ROADMAP.md` = phase log. `marketing/*` = the video/FB content kit.
- Build: `npm run build:worker`. Tests: `npm test` + `pytest backend/tests`. Deploy: `git push origin main`.

_End of handoff. Everything committed + pushed at `56d2145`. The build is done; the selling has just begun._
